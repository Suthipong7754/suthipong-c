#include <bits/stdc++.h>
using namespace std;
int main () {
int r;
double dr,de;
cin >> r;
dr = M_PI*pow(r,2);
de = 2*pow(r,2);
cout << fixed << setprecision(6) << dr << endl;
   cout << fixed << setprecision(6) << de << endl; 
}